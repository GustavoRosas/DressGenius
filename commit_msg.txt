fix: occasion i18n, image URLs, back button, usage null limit

- History + Home: occasion keys translated via i18n (wedding -> Casamento)
- APP_URL changed to 192.168.100.4:8000 (fixes image URLs for mobile)
- BackButton: bold chevron instead of thin arrow, all screens
- useUsage: analyses_limit can be null (premium), paywall check handles it
- UsageChip: null-safe limit check
