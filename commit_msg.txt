fix: onboarding loop after register - state not updating navigator

- OnboardingScreen now accepts onComplete callback
- RootNavigator passes setOnboardingDone(true) as onComplete
- Fixes: register -> redirected to onboarding -> stuck in loop
- completeOnboarding updates React state instead of just SecureStore
