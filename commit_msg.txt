fix: back button double render, refactor navigator options

- Added headerBackVisible: false to prevent native + custom back button
- Extracted screenWithBack shared options (DRY: 9 screens)
- Paywall keeps slide_from_bottom + modal presentation
- Fixes: tap back caused slide animation showing second back button
