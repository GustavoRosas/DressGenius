fix: PremiumContext syncs from backend /me on startup

- checkEntitlement now fetches GET /me and reads plan from backend
- Syncs isPremium + activatedAt to SecureStore
- Falls back to local SecureStore if offline
- Fixes: user was premium on backend but app showed free
